# Core dependencies for the AI Agent Ecosystem
jsonrpcserver==4.1.2
requests>=2.31.0
mcp

# (Optional) If you add Redis persistence later:
redis>=4.5.0

# (Optional) For the Streamlit dashboard:
streamlit>=1.25.0